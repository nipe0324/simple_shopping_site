<div class="page-header">
  <h1><%=t '.title' %></h1>
</div>

<%= form_for(resource, :as => resource_name, :url => session_path(resource_name),
	:html => { :class => 'form-horizontal' } ) do |f| %>

  <div class="control-group">
    <%= f.label :email, t('devise.column.email'), :class => 'control-label' %>
    <div class="controls">
      <%= f.email_field :email, :autofocus => true  %>
    </div>
  </div>

  <div class="control-group">
    <%= f.label :password, t('devise.column.password'), :class => 'control-label' %>
    <div class="controls">
      <%= f.password_field :password %>
    </div>
  </div>

  <div class="control-group">
    <div class="controls">
    	<%= f.check_box :remember_me %><%= f.label :remember_me, t('.rememver_me') %>
    </div>
  </div>

  <div class="form-actions">
    <p>Admin: admin@example.com / admin12345</p>
    <p>User : user@example.com  / user12345</p>
  </div>

  <div class="form-actions">
    <%= f.submit t('.sign_in'), :class => 'btn btn-primary' %>
  </div>
<% end %>

<%= render "devise/shared/links" %>
